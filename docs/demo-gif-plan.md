# Demo GIF Plan

The README should eventually include a short demo GIF showing the core value proposition.

## Storyboard

1. Open MCP inspector.
2. Select `gatekeeper_audit_diff`.
3. Paste a diff that adds a direct `fetch()` call.
4. Show Gatekeeper returning `Audit Results: FAIL`.
5. Paste a fixed diff using `httpClient`.
6. Show `Audit Results: PASS`.

## Capture requirements

- Keep terminal font readable.
- Avoid secrets or private paths.
- Keep the recording under 30 seconds.
- Use the sample payloads from `demo/run-local-audit.md`.

## README placement

Place the GIF immediately after the opening value proposition.
