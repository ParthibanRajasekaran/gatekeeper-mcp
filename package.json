{
  "name": "gatekeeper-mcp",
  "version": "0.1.0",
  "description": "MCP policy-as-code guardrails for AI-assisted software development.",
  "type": "module",
  "private": false,
  "license": "MIT",
  "bin": {
    "gatekeeper-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.build.json --noEmit"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "policy-as-code",
    "ai-governance",
    "secure-sdlc",
    "claude",
    "typescript"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
